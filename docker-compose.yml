version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: velzon_app
    restart: unless-stopped
    working_dir: /app
    ports:
      - "80:80"
    env_file:
      - .env.docker
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - velzon-network
    volumes:
      - ./storage:/app/storage
      - ./public/uploads:/app/public/uploads

  postgres:
    image: postgres:16-alpine
    container_name: velzon_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-velzon}
      POSTGRES_USER: ${DB_USERNAME:-velzon}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-velzon}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - velzon-network

  # Redis — only started when you explicitly enable the 'redis' profile:
  #   docker compose --profile redis up
  # Leave this off if you are using CACHE_DRIVER=database, QUEUE_CONNECTION=database, SESSION_DRIVER=database
  redis:
    image: redis:7-alpine
    container_name: velzon_redis
    restart: unless-stopped
    profiles:
      - redis
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - velzon-network

  # Optional: Extra Nginx for development with live reload
  #   docker compose --profile dev up
  nginx-dev:
    image: nginx:alpine
    container_name: velzon_nginx_dev
    restart: unless-stopped
    profiles:
      - dev
    ports:
      - "8080:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./public:/app/public:ro
    depends_on:
      - app
    networks:
      - velzon-network

volumes:
  postgres_data:
  redis_data:

networks:
  velzon-network:
    driver: bridge
